# Encrypt-Decrypt-Demo
This code shows the generalized process of encrypting/decrypting a short message using symmetric and asymmetric methods in python. It prints the original message, the encrypted message, the keys used, and the decrypted message.
